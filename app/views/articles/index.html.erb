<table>
<thead>
  <tr>
    <th>Title</th>
    <th>Created AT</th>
  </tr>
</thead>
<tbody>
<% @articles.each do | article | %>
  <tr>
    <td><%= article.title %></td>
    <td><%= article.created_at %></td>
  </tr>
<% end %>
</tbody>
</table>

<%= link_to "Create an article", new_article_path %>